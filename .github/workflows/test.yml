{"name": "Build and Test Python Code", "on": [{"push": {"branches": ["main"]}}], "jobs": [{"name": "Test Python Code", "runs-on": "ubuntu-latest", "steps": [{"name": "Checkout code", "uses": "actions/checkout@v2"}, {"name": "Install dependencies", "run": "pip install -r requirements.txt"}, {"name": "Run tests", "run": "python -m unittest discover -s . -p "test*.py""}, {"name": "Always succeed", "run": "echo 'Tests passed!'"}]}]}